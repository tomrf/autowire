{
    "name": "tomrf/autowire",
    "type": "library",
    "description": "autowire dependency injection from PSR-11 containers",
    "keywords": [
        "dependency injection",
        "autowire",
        "reflection",
        "psr-11",
        "container"
    ],
    "homepage": "http://github.com/tomrf/autowire",
    "license": "MIT",
    "authors": [
        {
            "name": "Tom Rune Flo",
            "email": "tom@x86.no",
            "homepage": "http://github.com/tomrf/"
        }
    ],
    "extra": {
        "long-description": [
            "Simple library to facilitate autowired instantiation of classes with injectable dependencies using the PHP reflection API.",
            "",
            "Dependencies are fetched and autowired from one or more PSR-11 containers implementing the ContainerInterface."
        ],
        "example": [
            "$autowire = new \\Tomrf\\Autowire\\Autowire();",
            "$autowire->addContainer($myDependencyInjectionContainer); // any PSR-11 container",
            "",
            "$instance = $autowire->instantiateClass(MyClassWithDependencies::class);"
        ]
    },
    "require": {
        "php": "^8.0",
        "psr/container": "^2.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "phpstan/phpstan": "^1.6",
        "friendsofphp/php-cs-fixer": "^3.8",
        "roave/security-advisories": "dev-latest"
    },
    "autoload": {
        "psr-4": {
            "Tomrf\\Autowire\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tomrf\\Autowire\\Test\\": "tests/"
        }
    },
    "scripts": {
        "phpunit": [
            "phpunit --do-not-cache-result"
        ],
        "phpstan": [
            "phpstan analyze --level max src"
        ],
        "cs-fixer": [
            "php-cs-fixer fix --allow-risky=yes --diff --verbose src"
        ],
        "test": [
            "@phpunit",
            "@phpstan"
        ]
    }
}
